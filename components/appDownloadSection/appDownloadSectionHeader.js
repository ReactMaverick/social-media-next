import styles from './appDownloadSectionHeader.module.css';

export default function AppDownloadSectionHeader() {
    return (

        <h1
            className={`${styles.sectionTitle} ${styles.slideDown} ${styles.appear}`}
        >
            download
        </h1>

    );
}
